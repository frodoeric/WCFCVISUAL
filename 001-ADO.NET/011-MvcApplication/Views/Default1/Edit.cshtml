@model _011_MvcApplication.Models.PESSOA

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PESSOA</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.COD_PESSOA)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.COD_PESSOA)
            @Html.ValidationMessageFor(model => model.COD_PESSOA)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.NOME_PESSOA)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NOME_PESSOA)
            @Html.ValidationMessageFor(model => model.NOME_PESSOA)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SALARIO_PESSOA)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SALARIO_PESSOA)
            @Html.ValidationMessageFor(model => model.SALARIO_PESSOA)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
